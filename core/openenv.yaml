name: TRY_ENV
description: Environment with three real-world tasks
tasks:
  - name: email_triage
    difficulty: easy
    action_space: category
    observation_space: subject, body, sender, timestamp
  - name: data_cleaning
    difficulty: medium
    action_space: field, new_value
    observation_space: raw_entry dict
  - name: scheduling
    difficulty: hard
    action_space: field, new_value
    observation_space: raw_request string + current_values dict
